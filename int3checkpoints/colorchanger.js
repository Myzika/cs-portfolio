//Variable that stores the DOM ID input (the HTML text input)
var textInput1 = document.getElementById("input");

//Variable that stores the DOM ID button (the HTML button)
var clickMe = document.getElementById("button");